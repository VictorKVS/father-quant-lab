# RUN-M0-S6-ATTR-20260817 — приёмочный протокол

## IDEA → SOURCE → HYPOTHESIS → REQUIREMENT

Уже полученный результат `BOT-RULE-101` соединяется с причинными метками только для
объяснения. Гипотеза — join по timestamp воспроизводим и не требует передачи режима
стратегии. Требования и запреты записаны в `FQL-S6-ATTR-002`.

## DESIGN → CODE → TEST

ADR-0011 определяет интервальную доходность и ретроспективную границу. Реализованы
модуль, CLI и fail-closed проверки SHA, instrument, duplicate/unknown timestamp и
времени доступности. Полный набор: 66 тестов `OK`; `compileall` и
`git diff --check` — PASS.

## RUN → RESULT

| Группа | Интервалы | Compounded return | Худший интервал | С экспозицией |
|---|---:|---:|---:|---:|
| RANGE\|NORMAL | 4 | -0.4890% | -0.6679% | 2 |
| UP\|NORMAL | 3 | -0.2974% | -0.2974% | 1 |

Из 11 возможных интервалов размечено 7 (63.64%), warm-up оставил 4 unlabelled.
SHA результата `ef4118b1...29691`. Режимы не использованы для решений робота.

## CRITIQUE → DECISION → LESSON

Аналитик сохраняет отрицательные значения без сравнения режимов. Архитектор отделяет
диагностику от стратегии. Программист повторно запускает исходный робот без regime
input. Тестировщик подтвердил fail-closed join. Риск-контролёр сохраняет PAPER/LIVE
закрытыми. Критик запрещает выводы по 3–4 наблюдениям.

Решение: `ACCEPT_JOIN_MECHANICS_ONLY`; M0/S2 не повышать. Доказано механическое
соединение timestamps. Не доказаны причинность убытка, различие режимов,
статистическая устойчивость, доходность или PAPER/LIVE readiness.

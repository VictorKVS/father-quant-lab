# RUN-M0-S6-XVERIFY-20260817 — приёмочный протокол

## IDEA → SOURCE → HYPOTHESIS → REQUIREMENT

Принятый stress-pack перепроверяется не ещё одним вызовом основной функции, а
отдельной Decimal-реализацией. Требование `FQL-S6-XVERIFY-004` заранее фиксирует
поля, допуск `1e-12`, PASS и блокирующие условия.

## DESIGN → CODE → TEST

ADR-0013 запрещает импорт основного классификатора и fixture generator. Oracle
повторно строит closes, log returns и RMS. Полный набор — 77 тестов `OK`; намеренно
подменённые trend и RMS дают `BLOCKED`; неверный suite ID отклоняется fail-closed.

## RUN → RESULT

| Метрика | Результат |
|---|---:|
| Сценарии | 4 |
| Метки | 28 |
| Проверенные поля | 196 |
| Расхождения | 0 |

Статус `PASS_INDEPENDENT_LABEL_EQUIVALENCE`. Result SHA-256:
`9fd55b9722c97e0172aea4cb9ad21492a1da02ba96cd36378ae95e58db3c1e3c`.

## CRITIQUE → DECISION → LESSON

Аналитик принимает только совпадение контракта. Архитектор отделяет oracle от
production module. Программист сохраняет независимую арифметику. Тестировщик
подтвердил обнаружение подмен. Риск-контролёр оставил PAPER/LIVE закрытыми. Критик
указывает на общую спецификацию и искусственные входы.

Решение: `ACCEPT_IMPLEMENTATION_EQUIVALENCE_ONLY`; M0/S2 не повышать. Доказано
совпадение двух реализаций на зарегистрированных fixtures. Не доказаны правильность
самой модели, историческая accuracy, калибровка, доходность или готовность торговли.

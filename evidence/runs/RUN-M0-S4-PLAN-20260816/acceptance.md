# RUN-M0-S4-PLAN-20260816 — приёмочный протокол

## IDEA → SOURCE → HYPOTHESIS → REQUIREMENT

До исторического теста фиксируются точный SHA-256 данных, параметры робота и
временные участки. Основание — книжный маршрут и `research-methodology.md`.
Гипотеза: `FQL-S4-GATE-004` машинно обнаруживает подмену данных, неверный порядок,
пересечение, нарушение embargo и незапечатанный OOS. Требования записаны в
`FQL-S4-PLAN-002` до реализации.

## DESIGN → CODE → TEST

ADR-0008 отделяет предрегистрацию от допуска dataset и performance gate.
Реализованы `experiment_plan.py`, CLI и sealed JSON manifest. Полный набор — 48
тестов, `OK`; отдельно проверены позитивный путь и шесть fail-closed классов.
`compileall` и `git diff --check` — PASS.

## RUN → RESULT

- Dataset: 12 MODELLED баров; SHA-256 `0a03a901...90a3a`.
- Plan: SHA-256 `13b7e1bd...4759f`.
- Result: SHA-256 `9fda76de...e3054`.
- TRAIN/VALIDATION/OOS: 4/4/4 бара.
- Status: `VALID_MODELLED_ONLY`.
- Performance, PAPER и LIVE: запрещены.

## CRITIQUE → DECISION → LESSON

Аналитик принимает только проверку структуры. Архитектор отделяет gate от движка.
Программист реализовал минимальный модуль без зависимостей. Тестировщик подтвердил
48/48. Риск-контролёр оставил все торговые режимы закрытыми. Критик отмечает, что
четыре бара и нулевой embargo не имеют статистического смысла.

Решение: `ACCEPT_GATE_MECHANICS_ONLY`; зрелость остаётся M0/S2. Доказано, что
manifest защищён от проверенных структурных нарушений. Не доказаны качество и право
использования данных, корректный исторический горизонт, доходность, устойчивость и
готовность PAPER/LIVE. Следующий внешний blocker — допуск исторического dataset;
следующий безопасный внутренний шаг — формализовать критерии размера периодов и
режимные метки без открытия OOS.

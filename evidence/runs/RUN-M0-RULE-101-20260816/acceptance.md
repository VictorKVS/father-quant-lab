# RUN-M0-RULE-101-20260816 — приёмочный протокол

## IDEA → SOURCE → HYPOTHESIS → REQUIREMENT

Первый книжный правиловый baseline нужен до ML/LLM-роботов. Гипотеза ограничена
причинной и воспроизводимой механикой moving-average crossover. Требования,
альтернативы, критерии и риски записаны в `FQL-S4-MA-001`; параметры 3/5
зарегистрированы до запуска, оптимизация запрещена.

## DESIGN → CODE → TEST

ADR-0007 фиксирует long/flat, полное длинное окно и исполнение на следующем open.
Код расположен в `strategies.py` и `cli.py`. Unit-тесты проверяют неверные окна,
warm-up, смену состояния и независимость текущего сигнала от будущего бара.
Полный набор: 39 тестов, `OK`; повтор двух прогонов дал побайтово одинаковый отчёт.

Первый контроль из свежего клона после тестов дал отрицательный результат среды:
`No module named father_quant_lab`, потому что одноразовая среда не установила
пакет с `src`-layout, а `PYTHONPATH` был задан только для предыдущей команды.
Исправлена процедура, не код и не тесты: `PYTHONPATH=src` экспортирован на весь
прогон. Повтор: 39 тестов `OK`, CLI завершён, SHA совпали, worktree чистый.

## RUN → RESULT

Источник — только `MODELLED` sample, SHA-256
`0a03a90118472528066a69a1046bc4d691c8bf3a92f79e91b7f82a33aea90a3a`.
Отчёт SHA-256
`793c0397c0a2fda8bf5551dfe40514658cb496db2d6eafcd04c0688138531bec`.

| Робот | Доходность | Max drawdown | Сделки |
|---|---:|---:|---:|
| BOT-CTRL-000 | 0.000000% | 0.000000% | 0 |
| BOT-CTRL-001 | 0.982652% | 0.858779% | 6 |
| BOT-CTRL-002 | 0.837865% | 0.951475% | 2 |
| BOT-CTRL-003 | 0.418372% | 0.476644% | 4 |
| BOT-RULE-101 | -0.784994% | 0.963359% | 2 |

## CRITIQUE → DECISION → LESSON

Аналитик: результат не поддерживает торговое преимущество. Архитектор: единый
движок и интерфейс стратегии сохранены. Программист: реализован минимальный baseline
без оптимизатора. Тестировщик: критерии механики выполнены. Риск-контролёр: реальные
средства, credentials, PAPER и LIVE отсутствуют. Критик: 12 модельных баров не
доказывают рыночную закономерность, а случайный контроль здесь лучший.

Решение: `ACCEPT_AS_MECHANICAL_BASELINE_ONLY`, сохранить отрицательный результат,
не менять окна постфактум и не повышать M0/S2. Доказаны причинная механика,
сопоставимость и воспроизводимость. Не доказаны доходность, устойчивость,
out-of-sample, PAPER/LIVE readiness и применимость к историческому EUR/USD.
